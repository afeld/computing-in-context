{
  "name": "computing-in-context-site",
  "version": "1.0.0",
  "private": true,
  "description": "11ty build for Computing in Context pages",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
